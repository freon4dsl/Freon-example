scoper ExampleScoper for language Entity

isnamespace { EntityModelUnit, Entity, Method, SumExpression }

Entity {
    namespace_addition = self.baseEntity;
}

AppliedFeature {
	scope = typeof( container );
}

